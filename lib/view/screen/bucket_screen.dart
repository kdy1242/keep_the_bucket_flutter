
import 'package:flutter/material.dart';

class BucketScreen extends StatelessWidget {
  const BucketScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('bucket'));
  }
}
